import 'package:flutter/material.dart';

const Widget kIconClose = const Icon(
  Icons.close,
  size: 32.0,
);

const Color kMtsRedColor = Color(0xFFE30611);
const Color kMtsWhiteColor = Color(0xFFFFFFFF);
const Color kOverlayColor = Color.fromARGB(209, 39, 62, 84);
